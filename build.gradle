plugins {
    id 'org.springframework.boot' version '1.5.3.RELEASE'
}

jar {
    baseName = 'type-spring-boot'
    version =  '0.0.1-SNAPSHOT'
}

repositories {
    jcenter()
}

dependencies {
    compile("org.springframework.boot:spring-boot-starter-web:1.5.3.RELEASE")
}